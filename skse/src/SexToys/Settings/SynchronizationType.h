#pragma once

namespace Toys {
    namespace Settings {
        enum SynchronizationType : uint8_t {
            PLAYER,
            MALE,
            FEMALE,
            SCENE,
            INDIVIDUAL_SYNCHRONIZATION
        };
    }
}