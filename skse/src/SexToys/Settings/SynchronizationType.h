#pragma once

namespace Toys {
    namespace Settings {
        enum SynchronizationType {
            PLAYER,
            MALE,
            FEMALE,
            SCENE,
            INDIVIDUAL_SYNCHRONIZATION
        };
    }
}