#pragma once

namespace Graph {
    namespace Action {
        namespace Peak {
            struct PeakTypeParams {
                // we don't need anything here yet
                // this exists anyways so we don't have to fiddle it in later if we ever do need it
            };
        }
    }
}